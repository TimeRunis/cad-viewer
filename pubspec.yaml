name: cad_viewer
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  ffi: ^2.0.0
  path_provider: ^2.1.0
  file_picker: ^6.1.1  # 用于选择文件
  fast_gbk: ^1.0.0
  dxf: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
